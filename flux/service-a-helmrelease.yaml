apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: service-a
  namespace: default
spec:
  chart:
    spec:
      chart: service-a
      sourceRef:
        kind: GitRepository
        name: repo
        namespace: flux-system
        # path: ./charts/service-a
  interval: 5m
  values:
    replicaCount: 1
